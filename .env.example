# Used only by generate:paths / resolve:final (needs a reachable API).
# Staging API is not publicly exposed — use local API for path generation.
BASE_API_URL=http://localhost:8080/api/v1
# Used by Playwright tests (env + path). Can point at staging FE.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
